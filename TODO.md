# NSLSP To Do Items

1. Allows print("some string") without sqigs -- wrong
2. Insists on optional args, e.g.
   set prompt_envelope = tool.aeiou.ComposeEnvelope(userdata_json, actions_stub, "", "")
3. Text rendering in pop up help is just not pretty

